# Copy this file to .env and set a real value for local development.
# On Render, set this as an Environment Variable in the service settings instead —
# don't commit the real passcode to the repo.
CLASS_PASSCODE=changeme
